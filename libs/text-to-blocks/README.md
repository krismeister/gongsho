# text-to-blocks

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build text-to-blocks` to build the library.

## Running unit tests

Run `nx test text-to-blocks` to execute the unit tests via [Jest](https://jestjs.io).
